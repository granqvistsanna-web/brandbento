/**
 * Layout Constants
 *
 * Breakpoint thresholds shared across the layout system.
 * Tile placement and grid definitions live in bentoLayouts.ts.
 */

/** Breakpoint thresholds in px — shared with bentoLayouts.ts */
export const BREAKPOINTS = {
  mobile: 0,
  tablet: 768,
  desktop: 1024,
} as const;
